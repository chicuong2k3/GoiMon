mutation CreateCombo($name: String!, $price: Decimal!, $imageUrl: String) {
  createCombo(input: { name: $name, price: $price, imageUrl: $imageUrl }) {
    id
    name
    price
    imageUrl
  }
}

mutation UpdateCombo($id: UUID!, $name: String, $price: Decimal, $imageUrl: String) {
  updateCombo(input: { id: $id, name: $name, price: $price, imageUrl: $imageUrl }) {
    id
    name
    price
    imageUrl
  }
}

mutation DeleteCombo($id: UUID!) {
  deleteCombo(id: $id)
}

mutation AddComboItem($comboId: UUID!, $productId: UUID!, $qty: Int!) {
  addComboItem(input: { comboId: $comboId, productId: $productId, qty: $qty }) {
    id
    items {
      id
      qty
      productId
      product {
        id
        name
        price
      }
    }
  }
}

mutation RemoveComboItem($comboId: UUID!, $itemId: UUID!) {
  removeComboItem(input: { comboId: $comboId, itemId: $itemId }) {
    id
    items {
      id
    }
  }
}
