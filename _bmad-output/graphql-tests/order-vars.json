{
  "query": "mutation CreateOrder($input: OrderInput!) { createOrder(input: $input) { id items { id productId qty unitPrice } } }",
  "variables": {
    "input": {
      "items": [
        {
          "productId": "REPLACE_WITH_PRODUCT_ID",
          "qty": 2,
          "unitPrice": 1.5
        }
      ]
    }
  }
}
